language: python
python:
  - "2.6"
  - "2.7"

# command to install dependencies
install:
  - pip install zc.buildout
  - python bootstrap.py
  - bin/buildout
   
# command to run tests
script: bin/test.sh
